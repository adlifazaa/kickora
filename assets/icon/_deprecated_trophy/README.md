# Deprecated trophy icons — do not use

These files are archived for reference only. They must not be referenced in
`pubspec.yaml`, Play Console, or generated launcher resources.

## Production replacement

Keep approved art in `assets/branding/final/app_icon_1024.png`, then run:

```
dart run flutter_launcher_icons
```

See `assets/branding/final/README.md`.
